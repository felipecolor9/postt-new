#APP environment with H2

app:
  message:
    This is a property file to the ${spring.application.name}

spring:
  jpa:
    hibernate:
      ddl-auto:
        create
  datasource:
    url:
      jdbc:h2:mem:postt
    username:
      admin
    password:
    driver-class-name:
      org.h2.Driver
server:
  port:
    8081